import React,{useState} from "react"; export default function Login({onLogin}){const[user,setUser]=useState("");const[pass,setPass]=useState(""); function submit(){if(!user||!pass)return;onLogin({user,time:Date.now()})} return <div className="loginScreen"><div className="loginCard"><div className="badge">v3.4</div><h1>IPTV Mobile</h1><p className="muted">Sauberer Session-Start für die App.</p><input placeholder="Benutzername" value={user} onChange={e=>setUser(e.target.value)} /><input placeholder="Passwort" type="password" value={pass} onChange={e=>setPass(e.target.value)} /><button className="primary wide" onClick={submit}>Einloggen</button></div></div> }