import React from "react"; export default function PosterCard({item,onClick,compact=false}){return <button className={`posterCard ${compact?"posterCardCompact":""}`} onClick={onClick}><img src={item.cover} alt={item.title} /><div className="posterTitle">{item.title}</div><div className="posterMeta">{item.category}</div><div className="progressLine"><div style={{width:`${item.progress||0}%`}} /></div></button> }