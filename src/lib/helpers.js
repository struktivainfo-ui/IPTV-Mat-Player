export function normalizeBaseUrl(url){return String(url||"").trim().replace(/\/+$/,"")} export function buildApiUrl(server,username,password,action){const base=normalizeBaseUrl(server);return `${base}/player_api.php?username=${encodeURIComponent(username)}&password=${encodeURIComponent(password)}&action=${action}`} export function buildLiveUrl(server,username,password,streamId,ext="m3u8"){const base=normalizeBaseUrl(server);return `${base}/live/${encodeURIComponent(username)}/${encodeURIComponent(password)}/${streamId}.${ext}`} export function buildMovieUrl(server,username,password,streamId,ext="mp4"){const base=normalizeBaseUrl(server);return `${base}/movie/${encodeURIComponent(username)}/${encodeURIComponent(password)}/${streamId}.${ext}`} export function buildSeriesUrl(server,username,password,streamId,ext="mp4"){const base=normalizeBaseUrl(server);return `${base}/series/${encodeURIComponent(username)}/${encodeURIComponent(password)}/${streamId}.${ext}`} export async function fetchJsonWithTimeout(url,timeoutMs=12000){const controller=new AbortController();const timeout=setTimeout(()=>controller.abort(),timeoutMs);try{const res=await fetch(url,{signal:controller.signal});if(!res.ok)throw new Error(`HTTP ${res.status}`);return await res.json()}catch(error){if(error?.name==="AbortError")throw new Error("Zeitüberschreitung beim Laden.");throw error}finally{clearTimeout(timeout)}} export function safeTop(list,max=40){return Array.isArray(list)?list.slice(0,max):[]}